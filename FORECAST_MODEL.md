# ต้นแบบคาดการณ์ปริมาณน้ำ X.173A และ X.90

ส่วนคาดการณ์ในเว็บแอปใช้โมเดล direct-horizon รายวันจากชุดวิเคราะห์
`Hydro_X90_Analysis_Package.zip` โดยมีสมการแยกสำหรับ +1, +2 และ +3 วัน

## ไฟล์ที่เกี่ยวข้อง

- `index.html` — แท็บคาดการณ์ กราฟรายวัน/รายชั่วโมง กราฟอ้างอิง และดาวน์โหลด CSV
- `forecast-model.js` — ตัวคำนวณ direct-horizon, แปลง Rating Table และปริมาตรน้ำ
- `data/forecast-model.json` — ค่าสัมประสิทธิ์ ผลทดสอบ รุ่นแบบจำลอง และข้อจำกัด
- `data/rating-tables.json` — ตาราง H–Q ของ X.173A และ X.90 ทุก 0.01 เมตร
- `data/rainfall-runoff-reference.json` — จุดกราฟฝนรวมรายเหตุการณ์กับ direct runoff depth หลังแยก baseflow
- `tests/test-forecast-model.js` — ตรวจผลคำนวณ Rating Table ปริมาตร รายชั่วโมง และ Q ≥ 0

## ข้อมูลนำเข้า

- Q ปัจจุบันของ X.173A และ X.90 หน่วย ม³/วินาที
- API ณ วันออกพยากรณ์ หน่วย มม. โดย `API(t)=P(t)+0.88×API(t−1)`
- ฝนเฉลี่ยลุ่มน้ำ P(t−2), P(t−1), P(t)
- ฝนพยากรณ์ P̂(t+1), P̂(t+2), P̂(t+3)

สมการแต่ละ horizon ใช้ Q และ API ที่จุดตั้งต้นเดียวกัน แล้วใช้ฝนพยากรณ์ถึงวันเป้าหมาย
จึงไม่ใช่การนำผล +1 วันไปคำนวณ +2 วันแบบ recursive

## ผลทดสอบที่นำมาแสดง

- X.90 test NSE: +1 = 0.865, +2 = 0.716, +3 = 0.631
- X.173A test NSE: +1 = 0.937, +2 = 0.853, +3 = 0.805
- หน้าเว็บแสดง test RMSE กำกับแต่ละค่า แต่ RMSE ไม่ใช่ prediction interval
- แสดง persistence baseline ซึ่งเท่ากับ Q ณ จุดตั้งต้น
- แสดง Q เป็นปริมาตรด้วย `ล้าน ม³/วัน = Q × 86,400 / 1,000,000`
- กราฟ 72 ชั่วโมงทำซ้ำค่าเฉลี่ยของแต่ละวัน 24 ครั้ง และแสดงปริมาตรต่อชั่วโมงใน tooltip

## Rating Table

- X.173A ใช้ `S.HC 2010/Y2025` ช่วง 9.40–18.00 ม.รทก. (0–1,950 ม³/วินาที)
- X.90 ใช้ `S.HC 2004/Y2025` ช่วง 2.00–13.05 ม.รทก. (0–5,110 ม³/วินาที)
- ทั้งสองตารางมีช่วงใช้ 1 เม.ย. 2568–31 มี.ค. 2569
- ใช้ interpolation เชิงเส้นระหว่างแถวติดกัน และไม่ extrapolate นอกช่วง

## ข้อจำกัดสำคัญ

- ผลประเมินเป็น hindcast ที่ใช้ฝนอนาคตจริงแทนฝนพยากรณ์ ผลใช้งานจริงจึงอาจต่ำกว่า
- ฝนของ X.173A ยังใช้น้ำหนัก Thiessen ของลุ่ม X.90 เพราะไม่มีขอบเขตลุ่มย่อยแยก
- ช่วงปี 2024–2025 มีน้ำหนักสถานีฝนที่วัดจริงต่ำ ไม่ควรใช้เป็นผลยืนยันหลัก
- ค่าโทรมาตรระดับน้ำหน่วย ม.รทก. ไม่ใช่ Q จึงต้องแปลงด้วย Rating Table ก่อน
- ตารางที่ได้รับหมดช่วงใช้แล้วในวันที่ 31 มี.ค. 2569 การแปลงปัจจุบันจึงเป็นข้อมูลอ้างอิงจนกว่าจะมีฉบับใหม่
- กราฟรายชั่วโมงเป็นการกระจายค่าเฉลี่ยรายวัน ไม่ได้จำลองรูป hydrograph หรือยอดน้ำภายในวัน
- กราฟฝน–น้ำท่าเปลี่ยนเป็นรายเหตุการณ์เพื่อไม่ให้วันแล้งจำนวนมากครอบงำผล
- X.173A ใช้ทุกเหตุการณ์ที่ผ่านธงคุณภาพ ส่วน X.90 ใช้เหตุการณ์ฤดูฝนหลักเดือน พ.ย.–ม.ค.
- ไม่มีการลบ outlier เป็นรายจุดเพื่อเพิ่ม R² และกราฟยังเป็นสมการอ้างอิง ไม่ใช่สมการพยากรณ์หลัก
- ยังไม่มี prediction interval, เกณฑ์เตือนภัย และการทดสอบ rolling-origin ที่รับรอง
- ผลลัพธ์เป็นต้นแบบเพื่อการพัฒนา ไม่ใช่คำเตือนภัยทางการ

## อัปโหลดขึ้น GitHub Pages

ให้อัปโหลดหรือแทนที่อย่างน้อย 5 รายการโดยคงตำแหน่งเดิม:

1. `index.html`
2. `forecast-model.js`
3. `data/forecast-model.json`
4. `data/rating-tables.json`
5. `data/rainfall-runoff-reference.json`

ไม่ต้องสร้าง workflow ใหม่สำหรับส่วนคาดการณ์
