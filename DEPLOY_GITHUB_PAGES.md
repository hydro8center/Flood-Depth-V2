# อัปเดต GitHub Pages รุ่น Flood-fill 12 ก.ย. 2569

อัปโหลดไฟล์และโฟลเดอร์ทั้งหมดในชุดนี้ไปที่รากของ repository โดยให้ `index.html` อยู่ระดับบนสุดเหมือนเดิม

ไฟล์ใหม่หรือไฟล์ที่ต้องแทนของเดิมในรุ่นนี้:

- `index.html`
- `flood-fill.js`
- `data/river.geojson`
- `forecast-model.js`
- `hydrograph-analog.js`
- `hydrograph-analog-ui.js`
- `data/historical-hydrograph-analogs.json`
- `data/x174-unit-hydrograph.json`
- `data/x174-thiessen.geojson`
- `ref/flooddepth-forecast-methodology-v1.2.docx`
- `ref/flooddepth-forecast-methodology-v1.2.pdf`
- `ref/X174_ANALYSIS_REPORT.md`
- `tests/test-forecast-model.js`
- `tests/test-flow-gates.js`
- `tests/test-flood-fill.js`

รุ่นนี้เพิ่มแนวคลองและตลิ่งจาก `data/river.geojson` และใช้เกณฑ์เปิดพื้นที่บนแผนที่สองเงื่อนไขที่ทำงานแยกกัน:

- พื้นที่ริมคลอง ร.1 และชุมชนลุ่มต่ำฝั่งซ้ายคลองอู่ตะเภาเริ่มได้รับผลกระทบเมื่อ Q รวมก่อนไหลเข้าเมืองถึง 1,680 ม³/วินาที = คลอง ร.1 1,200 + ทาง X.44 480 ม³/วินาที
- พื้นที่ชุมชนฝั่งขวาคลองอู่ตะเภาและเขตเศรษฐกิจหาดใหญ่เริ่มได้รับผลกระทบเมื่อระดับสถานี X.44 ถึง 8.30 ม.รทก. และความเสียหายอาจรุนแรงขึ้นหากระดับน้ำเพิ่มต่อเนื่อง

หลังเปิดทางน้ำ `flood-fill.js` จะคงไว้เฉพาะเซลล์ต่ำที่เชื่อมต่อจากแนวคลอง ตัดแอ่งต่ำโดดเดี่ยวและป้องกันการลอดผ่านมุมของเซลล์แห้ง พื้นที่นอกช่วงแนวตลิ่งที่ยังไม่ทราบทางเชื่อมจะไม่ถูกแสดงเป็นน้ำท่วม

รุ่น 0.3.3 ขยายการแสดงผล X.174 จาก 72 เป็น 120 ชั่วโมง โดยฝนคาดการณ์ยังมี 3 วันเท่าเดิม เพื่อไม่ตัดยอดและปริมาตรน้ำท่าที่ตอบสนองหลังฝนวันที่สาม

รุ่น 0.3.4 ต้องแทน `data/x90-unit-hydrograph.json` พร้อม `index.html` เพราะปรับเทียบ X.90 ร่วมกับเหตุการณ์ พ.ย. 2567 และ 2568

รุ่น 0.4.0 เพิ่มการกรอกระดับน้ำรายชั่วโมง X.90/X.174 เพื่อเปรียบเทียบรูปทรงกับปีน้ำหลากในอดีตและประมาณช่วงเวลาถึงยอด รุ่น GitHub Pages ไม่มีการเรียกโทรมาตรอัตโนมัติ ผู้ใช้ต้องกรอกข้อมูลเอง เพื่อไม่ต้องเปิดเผยรหัสหรือพึ่งบริการฝั่งเครื่องแม่ข่าย
- `.gitignore`

ต้องคงโฟลเดอร์เดิม `assets/`, `data/`, `ref/`, `.github/workflows/` และไฟล์อื่นในชุดไว้ เพราะหน้าเว็บเรียกใช้ด้วย path แบบสัมพัทธ์

ไม่ต้องอัปโหลด `.DS_Store`, `__pycache__/`, ไฟล์ต้นฉบับใน Downloads หรือโฟลเดอร์ `work/x174-analysis/`
และห้ามคัดลอก `config/private.env` จากชุด PC/Mac ขึ้น GitHub

หลังอัปโหลด ให้เปิดแท็บ Actions และรอ `pages build and deployment` เป็นสีเขียว จากนั้นเปิด GitHub Pages แล้วกดรีโหลดแบบไม่ใช้ cache หากยังเห็นรุ่นเก่า
